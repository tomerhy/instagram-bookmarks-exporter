{
  "name": "saved-posts-library-export",
  "version": "4.4.5",
  "private": true,
  "description": "Chrome extension \"Saved Posts Library & Export\" — see manifest.json. This package.json exists only so `npm test` works; the extension itself ships zero npm dependencies, which is why there is no lockfile.",
  "scripts": {
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=18"
  }
}
